{
  "name": "buildmon",
  "version": "0.0.1",
  "author": "Vedran Burdzovic",
  "description": "View and interact with your project build history over time",
  "main": "dist/src/index.js",
  "license": "GPL-2.0-only",
  "repository": "https://github.com/buildmon/buildmon.git",
  "scripts": {
    "build": "tsc",
    "format": "prettier --no-semi --single-quote --write ./**/*.ts",
    "lint": "tsc --noEmit",
    "test": "jest"
  },
  "husky": {
    "hooks": {
      "pre-push": "npm run test"
    }
  },
  "devDependencies": {
    "@types/express": "^4.17.1",
    "husky": "^3.0.1",
    "jest": "^24.8.0",
    "prettier": "^1.18.2",
    "ts-jest": "^24.1.0",
    "typescript": "^3.5.3"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
